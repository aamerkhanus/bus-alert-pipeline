name: CI Smoke Test

on:
  push:
    branches: [ "main" ]
  workflow_dispatch: {}

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "✅ GitHub Actions is working!"
