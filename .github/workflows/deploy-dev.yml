name: Deploy Dev
on:
  push:
    branches: [ main ]
  workflow_dispatch:  # allows you to run manually from Actions tab

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read

    steps:
      - uses: actions/checkout@v4

      # --- AWS auth: use OIDC if no keys are provided ---
      - name: Configure AWS (OIDC)
<<<<<<< HEAD
        if: ${{ secrets.AWS_ACCESS_KEY_ID == '' }}
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::314119347236:role/github-actions-bus-alerts
          aws-region: us-east-1

      # --- AWS auth: use Access Keys if provided ---
      - name: Configure AWS (Access Keys)
        if: ${{ secrets.AWS_ACCESS_KEY_ID != '' }}
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
=======
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::314119347236:role/github-actions-bus-alerts
>>>>>>> 996cd59 (ci: fix workflows to use OIDC only (no secrets in if:))
          aws-region: us-east-1

      - name: Verify AWS identity
        run: aws sts get-caller-identity

      # --- Example deploy command (replace with real command) ---
      - name: Deploy Lambda or Terraform
        run: |
          echo "Deploying to AWS DEV environment..."
